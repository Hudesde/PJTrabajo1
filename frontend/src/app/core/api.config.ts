/**
 * URL base de la API REST del backend.
 *
 * En desarrollo el backend Spring Boot corre en el puerto 8080.
 * Si lo cambias (o despliegas en otro host), ajusta esta constante.
 */
export const API_BASE_URL = 'http://localhost:8080/api';
